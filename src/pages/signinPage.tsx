import React, { FC } from 'react';
import TopHeader from '../components/topHeader';

const SigninPage: FC = () => {
  return (
    <TopHeader>
      <div>SigninPage</div>
    </TopHeader>
  );
};

export default SigninPage;
