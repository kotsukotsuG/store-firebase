import React, { FC } from 'react';
import TopHeader from '../components/topHeader';

const MyPage: FC = () => {
  return (
    <TopHeader>
      <div>MyPage</div>
    </TopHeader>
  );
};

export default MyPage;
